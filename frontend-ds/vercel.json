{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  
  "buildCommand": "npm rebuild lightningcss --platform=linux --arch=x64 && npm run build",
  "outputDirectory": "dist"
}